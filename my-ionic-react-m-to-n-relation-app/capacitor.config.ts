import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-ionic-react-m-to-n-relation-app',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
